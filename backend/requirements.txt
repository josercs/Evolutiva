#############################################################
# Aggregador principal (produção mínima):
#   pip install -r requirements.txt
# Dev (com testes e auditoria):
#   pip install -r requirements-dev.txt
# Adicionar/ativar extras editando requirements-extra.txt
#############################################################

-r requirements-core.txt
-r requirements-extra.txt

# Observação:
# Não adicione libs diretamente aqui; use os arquivos split.
# Isso mantém o diff limpo e facilita auditoria.
